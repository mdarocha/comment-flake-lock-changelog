export async function getPullRequestRefs(prNumber: string): Promise<{ base: string; head: string }> {
    throw new Error("TODO!");
}

export async function getFileContentAtCommit(commit: string, path: string): Promise<string> {
    throw new Error("TODO!");
}
